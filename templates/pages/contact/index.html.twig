{% extends 'base.html.twig' %}

{% block title %}eRecipe - Contact
{% endblock %}

{% block body %}
	<div
		class="container">
		{# Message de succès #}
		{% for message in app.flashes('success') %}
			<div class="alert alert-success mt-4">
				{{ message }}
			</div>
		{% endfor %}

		<h1>Formulaire de contact</h1>

		{{ form(form) }}
		</div>
	{% endblock %}
