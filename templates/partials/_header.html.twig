{# le underscore devant le nom du fichier est pour montré que ce n'est pas une page #}

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">SymRecipe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_home') }}">Accueil
          </a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="{{ path('recipe_community') }}">Recette de la communauté
            </a>
          </li>
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('app_ingredient') }}">Ingrédients
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('app_recipe') }}">Recettes
            </a>
          </li>
        {% endif %}
      </ul>
      <div class="d-flex">
        <ul class="navbar-nav me-auto">
        {% if app.user %}
          <li class="nav-item dropdown" style="margin-right: 5rem">
              <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
              <div class="dropdown-menu">
                <a class="dropdown-item" href="{{ path('user_edit', {id: app.user.id}) }}">Modifier mes informations</a>
                <a class="dropdown-item" href="{{ path('user_edit_password', {id: app.user.id}) }}">Modifier mon mot de passe</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('security_logout') }}">Deconnexion</a>
              </div>
            </li>
        {% else %}
           <li class="nav-item">
            <a class="nav-link active" href="{{ path('security_login') }}">Connexion
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('security_registration') }}">Inscription
            </a>
          </li>
        {% endif %}
        
          
        </ul>
      </div>
    </div>
  </div>
</nav>